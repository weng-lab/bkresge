services:
  dev-container:
    image: autumnusomega/bioinformatics:spatial
    # build: .
    container_name: vscode-container-spatial
    volumes:
      - /zata:/zata
      - /data:/data
    stdin_open: true
    working_dir: /zata/zippy/kresgeb
    tty: true
    restart: unless-stopped